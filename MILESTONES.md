# Milestones

Concise one-liners. **`git log` is the source of truth.** This file exists so a new collaborator can scan the arc without diving into 300+ commits.

## 2026 (active development)

- **04-30 PR#43:** 2D → 3D voxel pivot.
- **05-03 to 12:** destructible voxel slice; Copper Isles demo; Combat v1 (Goblin/Spear/BloodVFX); Skills (PR#201); MP (PR#180); cubic generator C++.
- **05-13 to 16:** `HeightmapGeneratorBase` (#203); `Profiler.real_us` reliable (#207); `WaterChunkMesher` C++ (#214, later deleted).
- **05-18 native-fluid pivot:** water = `VoxelBlockyModelFluid` ids 16–23, 8 levels, mesher auto-slopes. Legacy id 5 kept for old saves. Plan: `design/WATER_STAGE6_PLAN.md`.
- **05-19/20:** UnderwaterFilter (instant-snap submerge, vol fog, god rays); AgX tonemap + SSAO/SSIL/PSSM/MSAA. **Never flip `bake_tangents`** (breaks water meshes).
- **05-21/22:** AudioManager + 548 SFX takes; GraphicsManager 5 tiers; procedural sky/clouds/stars; `AtmosphereProfile`; tangent-free `terrain_voxel.gdshader`.
- **05-22/26 PR#240:** DistantTerrain streaming heightmesh replaces baked HorizonSkirt. LOD outrun fixed. F11/F12 debug overlays.
- **05-27 PR#241:** Four C++ ports — `VoxelGravityCpp` (131→2.8 ms, **46× win**), `EmissiveLightCpp` v1, `EmissiveBakedCpp` (Phase J), `WaterFlowCpp`. All parity-gated. **Zylann byte layout is Y-fastest.**
- **05-27 PR#243:** LOD1+ water surface line FIX (horizon plane + UnderwaterFilter group-toggle + WaterDiag backtick / Shift+backtick / debug_mode 7-8).
- **05-27 PR#244:** Phase K bundle — selection outline, cloud-phase accumulator, DebugOverlay GRAPHICS sub-view, lens flare, rainbow (all toggle-gated).
- **05-27 PR#245 Weather rework (MERGED 2026-05-28):** designer playtest deferred all visuals — rain shader / wet terrain / splashes / god rays gated OFF by default; audio envelope crossfade live. Needs multi-session iteration. `design/WEATHER_REWORK_2026-05.md`.
- **05-27 PR#246 EntityStreamer (CLOSED — folded into #247):** `EntityRegistry` (per-chunk + JSON save/load) + 4-tier AI sleep (ACTIVE/AWAKE/SLEEPING/OFFLOADED). Goblin/NPC/VoxelDrop retrofitted. Headless `entity` gate 66 checks. Shipped to main via #247. `design/ENTITY_STREAMING.md`.
- **05-28 PR#247 Combat Phase 5 (MERGED 2026-05-30, squash `bc0d895`):** charged-spear (hold LMB ≥50 dmg) → gib explosion (12 GibChunks, radial impulse) + 0.15 s time-slow + camera kick + spear reparents to chunk. ThrowableHandler charge mechanic added (Phase 3 finish). Overkill threshold = 50 (re-raise once charged dmg passes 80 via perks). Includes the #246 EntityStreamer work. CombatTest debug-kill key is **F10** (F8 = editor Stop).
- **05-28 PR#239 Directional Melee v1 (MERGED 2026-05-30, squash `3936e94`):** sword + shield, four-direction mouse-flick attacks (**flick toward the blow's origin** — UP=overhead, DOWN=thrust, LEFT/RIGHT=that-side sweep; inverted from the first "sword moves where I flick" model on 2026-05-29), charged 2× + feint, RMB tap=parry / hold=directional block + `auto_block` toggle, `ParryChainTracker`, `EnemyAttackPool` telegraphs, `HUDDirectionArrows` + `HUDCombatRadar`. **Lock-on prototyped then removed — pure free-aim.** Phases 0-6. Gibs (#247) + melee coexist on `Goblin`/`Enemy3D`.
- **06-12 PR#249 Finite water sim + voxel physics + water polish (MERGED 2026-06-12):** third-gen water — `FiniteWaterCore` in-memory unit ledger is the single authority (post-mortem: both old sims read the async-written world as their own memory), conservation provable (`units == placed − evap − absorbed − merged − removed`), 18-voxel spread reach, oceans stay infinite-source (fill writes `SOURCE_BYTE`, only SOURCE feeds it). Bucket pours/scoops conserved units; height-aware wade-vs-swim; sim-written DIR currents + `RiverFlowVolume`; tree-sever follow (whole tree = one cluster); buoyancy via `density_relative_to_water` (logs float/raft, stone sinks); caustics (default OFF) + `WaterBiomeZone` fog + underwater audio muffle. New headless gates: `finite`, `finite_world` (real-scene end-to-end), `sever`; Linux harness + GDExtension build enabled. W6 C++ port deferred until a capture spikes. Designer verdict: water accepted in-engine; tree-sever/buoyancy acceptance deferred — no trees in World3D yet. `design/WATER_FINITE_SIM_PLAN.md`.
- **06-12 PR R4 Micro-voxel flora v1:** real destructible voxel grass + flowers (the Lay-of-the-Land flora look). Three CHANNEL_TYPE ids — `grass_blade`(24)/`flower_red`(25)/`flower_blue`(26) — as cross-quad `VoxelBlockyModelMesh` models injected at runtime in `World3DBootstrap` right after the water fluids (16..23). C++ cubic generator scatters them deterministically on grassland at LOD0 (~35% grass, ~2% flowers; new `grass_blade/flower_red/flower_blue_material_id` + `flora_seed` props, default 0 = off, wired by the bootstrap). Walk-through (no collision), destructible (carve removes via TYPE writes). Sim-correct: water flows in + overwrites flora (`_finite_is_solid` skips it), gravity/sever ignore flora (GD ref + C++ `voxel_gravity_cpp` + `SeverFollowLib` all skip the 24..26 range — parity held). New `scripts/FloraMaterial.gd` (`is_flora()`, no class_name, water-helper analogue) + `VoxelMaterialRegistry.is_flora()` wrapper + 3 `.tres` materials. New headless `flora` gate (id classification / water-displaces-flora / gravity+sever exclusion w/ C++ parity / generator determinism + no-flora-at-LOD>0); `phase2` updated to expect 27 models. Deferred: trample, scythe drops + XP, wind sway, biome density, pixel-art tiles (flat vertex-colour quads for v1). `design/VISION_VOXEL_10CM.md`.
- **06-12 PR#251 10cm re-architecture — "Lay of the Land" (in flight):** `VoxelScale.gd` single source of truth (VOXELS_PER_METER=10, VOXEL_SIZE_M=0.1; headless `scale` gate); terrain scale flip (transform 0.166667→0.1, sea level 72→120 vox, Y bounds −512..1024, gravity `max_cluster_voxels` 4096→16384, emissive cell 4→7, delta DB renamed `voxel_deltas_v10.sqlite`); streaming/LOD retune (view_distance 864 vox, lod_count 5, `collision_lod_count=3` → terrain collision extends to ~51.2 m); mining rework (physical-volume swing-time anchor, scroll-wheel S/M/F carve presets, destroy-preview face highlights; headless `mining` gate); micro-voxel flora R4 (`FloraMaterial.gd`, `FloraMeshBuilder.gd`; ids 24–26); far-grass impostors + wind sway (`FarGrassManager.gd`, `flora_sway.gdshader`, default ON); micro-detail D1–D4 (pebbles/twigs ids 27–28, dug-wall grain, grass trample, `WaterFoamManager.gd` default OFF); distant skirt atlas-sampled palette + AO darken (no pale seam); fog tune. `design/VISION_VOXEL_10CM.md`.
