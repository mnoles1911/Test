// MiraThalVoxelBake.cpp — module boilerplate.
//
// Every Unreal module needs exactly one IMPLEMENT_MODULE so the engine can load
// it. This module has no startup/shutdown work of its own, so we use the engine's
// stock empty implementation (FDefaultModuleImpl). All the real work lives in
// VoxelNaniteBaker.cpp as a plain library of static functions.
#include "Modules/ModuleManager.h"

IMPLEMENT_MODULE(FDefaultModuleImpl, MiraThalVoxelBake);
