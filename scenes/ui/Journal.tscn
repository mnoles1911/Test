[gd_scene load_steps=2 format=3]

; JournalUI — bare CanvasLayer. JournalUI.gd builds all child nodes in _ready()
; so there is nothing to maintain here. The old 320×180 hardcoded layout nodes
; have been removed; the script creates anchor-relative nodes instead.

[ext_resource type="Script" path="res://scripts/JournalUI.gd" id="1"]

[node name="JournalLayer" type="CanvasLayer"]
layer = 10
script = ExtResource("1")
