# Lessons Learned

Date | Problem | Fix | PR
---|---|---|---
2026-04-29 | `delta` unused warning in `_physics_process` | Rename to `_delta` | #27
2026-04-29 | `offset` shadows `PointLight2D.offset` in CampfireFlicker | Rename to `energy_offset` | #27
2026-04-29 | E key trigger did nothing | `Input.is_action_just_pressed()` requires editor-configured action; switched to `_unhandled_input` + direct `KEY_E` check | #27
2026-04-29 | `Engine.has_singleton("Dialogic")` always false | That API is for C++ singletons only; use `get_node_or_null("/root/Dialogic")` for autoloads | #29
2026-04-29 | `timeline_ended` crash on CanvasLayer | `Dialogic.start()` returns the layout node, not a timeline; connect `timeline_ended` on the `Dialogic` autoload with `CONNECT_ONE_SHOT` | #30
2026-04-29 | Dialogic dialogue box overflows 320x180 viewport | Default Dialogic style is sized for 1080p. In Dialogic → Styles, set box width ~280px, height ~60-70px, anchored bottom-center. Font size 6-8px (renders crisp at 4x scale). Configure via editor only — not a code fix. | —
2026-04-30 | `move_toward(Vector2.ZERO, SPEED)` causes instant stop | Step arg is per-call, not per-second. Player hit zero velocity in one frame. Add a `DECEL` constant and multiply by `delta` so braking is frame-rate independent. | #34
2026-04-30 | ColorRect (Control node) drifts from world-space siblings | Control nodes anchor to screen/viewport space. When Camera2D moves, a ColorRect placed under Node2D shifts visually while Node2D siblings stay fixed. Use Polygon2D or Sprite2D (Node2D subclasses) for world-space placeholder visuals. | #34
2026-04-30 | PointLight2D light bleeds through walls | `CollisionShape2D` blocks physics but NOT light. Add `LightOccluder2D` + `OccluderPolygon2D` to walls so 2D light is contained. | #36
2026-04-30 | Radial light origin offset by ~128px (light "from outside" the campfire) | `GradientTexture2D` `fill_from` defaults to `(0,0)` (top-left) — NOT center. With `fill = 1` (radial), the bright spot lands at the texture's top-left, not its middle. Always set `fill_from = Vector2(0.5, 0.5)` and `fill_to = Vector2(1, 0.5)` for centered radial gradients used as light textures. | #37
2026-04-30 | 2D→3D coordinate confusion: `Input.get_vector` returns Vector2(x, y) but in 3D, the floor is the **XZ plane** with Y as up. | Map `input_dir.x → velocity.x`, `input_dir.y → velocity.z`. Don't try to use `input_dir.y → velocity.y` — that would launch the player into the air. | M4-3D
2026-04-30 | Camera clipping through walls in 3D | Use `SpringArm3D` as the camera mount (not a bare Camera3D). `SpringArm3D` casts a sphere from the pivot toward the camera position and shortens the arm if anything blocks it — built-in wall avoidance, no script needed. | M4-3D
2026-04-30 | `PointLight2D.energy` flicker logic doesn't compile under `OmniLight3D` | The 3D property is `light_energy`, not `energy`. Math is identical, only the property name changes. | M4-3D
2026-04-30 | `CollisionShape3D` placed at the origin makes a CharacterBody3D's feet sink into the floor by half its height | A capsule shape's origin is its **center**. For a 1.7 m tall capsule, set the CollisionShape3D's local Y position to `+height/2` (e.g. 0.85) so the bottom of the capsule sits on Y=0. Same for the visual mesh. | M4-3D
2026-05-03 | `VoxelMesherCubes` showed untouched terrain even after edits drained successfully through `VoxelEditManager._apply_edit` | Cubes reads `VoxelBuffer.CHANNEL_COLOR`, not `CHANNEL_SDF`. Edits writing to SDF were correct on disk but invisible because the mesher never read them. Fix: `tool.channel = CHANNEL_COLOR`, `tool.mode = MODE_SET`, `tool.value = packed_rgba` (0 = alpha-zero = carved air). | #102
2026-05-03 | `VoxelGeneratorScript` produced "Central buffer must be valid" errors thousands of times at world load | The script must override `_get_used_channels_mask()` to declare which channels it writes (`return 1 << VoxelBuffer.CHANNEL_COLOR` for Cubes-mesher generators). Without it, Zylann assumes default-SDF and never allocates the channel the mesher actually reads. | M5-3D bring-up
2026-05-03 | `VoxelTool.do_sphere(world_pos, world_radius)` carved at 1/8 the size and 8× closer to origin than expected | The tool takes **voxel-grid coords**, not world. With `terrain.transform.scale = 0.125` (8 vox/m), pass coords through `terrain.to_local()` and multiply radii by `1.0 / scale.x` before calling `do_sphere` / `do_box`. | #103
2026-05-03 | `var x := arr[0]` fails parse with "Cannot infer type" | `Array` returns `Variant` for index access, and the walrus operator can't infer from `Variant`. Type-annotate explicitly: `var x: Node = arr[0]`. Same applies to `Dictionary` value access. | #102
2026-05-03 | `var basis: Basis = ...` triggers SHADOWED_VARIABLE_BASE_CLASS warning in any `Node3D` subclass | `Node3D.basis` is an inherited property. Rename local variables — `cam_basis`, `local_basis`, `body_basis`. | #102
2026-05-03 | `TransitionManager.go_back()` did nothing on first launch from `MainMenu` | `change_scene()` only pushed `GameState.current_scene` to history when non-empty, but on first run that field starts empty. Fall back to `get_tree().current_scene.scene_file_path` so the initial scene always lands in history. | #100
2026-05-03 | `Settings.tscn` sliders and buttons completely unresponsive | Dialogic's input subsystem consumes `InputEventMouseButton` events project-wide before Godot's `_gui_input` dispatcher fires. Three UIs already work around this with manual `_input` + `Rect2.has_point()` dispatchers (`MainMenu`, `PauseMenu`, now `Settings`). Standard `Button.pressed` / `HSlider` drag will not work in this project — implement manual hit detection. | #100
2026-05-03 | `Settings` mouse cursor invisible / clicks register at wrong screen position | `PauseMenu._close()` sets `Input.mouse_mode = MOUSE_MODE_CAPTURED` before transitioning to Settings. Any UI scene reached from PauseMenu must explicitly set `Input.mouse_mode = MOUSE_MODE_VISIBLE` in its `_ready()`. | #100
2026-05-03 | `PowderCharge` carved at 2 m radius even after `voxel_aoe_radius = 10.0` was set in `ITEM_REGISTRY` | `ThrowableHandler` instantiated the scene but never read inventory stats, so the spawned RigidBody3D kept its `@export` defaults. When data lives in a registry but is consumed by an instance, the registry-to-instance copy step is silent if missing — verify it explicitly. Pattern: after `instantiate()`, `for key in ITEM_REGISTRY[id]: if key in instance: instance.set(key, ...)`. | #105
2026-05-03 | Explosives "didn't detonate" — log showed BOOM but player saw nothing | Detonation had zero on-screen feedback; player read large invisible carve as "didn't work." Every gameplay event needs visual feedback before the carve/damage/etc. lands — light flash + emissive sphere with a tween-driven fade is enough as a placeholder until real VFX. | #106
2026-05-03 | Throwables flew flat-forward regardless of camera pitch | Spawn used `_player.transform.basis.z` (player body forward, horizontal-only) plus a hardcoded `+UP * 0.3` bias, so aiming up and aiming down produced the same throw. Read `-camera.global_transform.basis.z` for direction (carries pitch); spawn position can stay anchored to body forward at chest height so the charge doesn't materialize at Roland's feet when looking sharply down. | #108
2026-05-03 | Player walked on water plane / water plane buried under terrain | Logic was correct (`feet_y <= surface_y`); world geometry was wrong. Terrain biased `+10 m` for accessible spawn put average ground at Y=10 while ocean was at Y=0 — water plane invisible under terrain. Raise water elevation to match the biased terrain (Y=6 puts low ground in valleys underwater while flat ground stays dry). When swim physics "doesn't work," check world Y values before assuming a code bug. | #109
2026-05-03 | Saves loaded against a baseline-changed generator floated in mismatched terrain | Bumped `VoxelEditManager.WORLD_GENERATOR_VERSION` is non-negotiable for every shape-affecting change to `CubicHeightmapGenerator` (height range, offset, noise layers, quantization, color mode). `GameState.save_game` stamps the version; `load_game` refuses the load on mismatch. There is no silent migration. | M5-3D bring-up
2026-05-03 | Designer iteration loop on terrain look took ~10 min/iteration via PR-and-rebuild | Add `@tool` + `@export_range(min, max, step)` to expose every numeric knob as Inspector slider. Add a `Preset` enum with a setter that batch-writes a stored param dict — guard against re-entrance with a `_applying_preset: bool` flag, otherwise the inner `set()` calls re-fire the setter. Designer F5s once and tunes live; `git` is only invoked when baking a chosen preset. | #109
2026-05-03 | Carve sphere "stopped working" after 3-4 throws at one spot | Not a bug. Each carve writes 0 (air) into voxels in its sphere; second carve into the same crater is writing air into voxels that are already air. Log shows the carve completing because the queue drained — the operation is just a no-op when the target voxels are already air. Throw at fresh terrain to confirm. | M5-3D bring-up
2026-05-05 | Mining 3×3×3 carve collapsed to 1×1×1 on some voxels | `_terrain.to_local()` does a float matrix transform; with `transform.scale = 0.166667` (1/6 m), world `-0.166667` round-trips to `-0.999999...` rather than `-1.0`. `Vector3i` truncation then loses one voxel per edge, collapsing the 3×3×3 to 1×1×1. Fix: keep the carve in voxel-grid integers from the start (`floori(world * 6)`), don't go through `to_local()` on the hot path. Added `VoxelEditManager.queue_edit_box_voxels(min_v: Vector3i, max_v: Vector3i, value: int)` and updated `_carve` to call it. | #134
2026-05-05 | Right-click smoothing produced no work on untouched terrain on first load | Zylann streams chunks for rendering before `CHANNEL_COLOR` is fully populated, so `tool.get_voxel()` returns 0 (air) on chunks the player can clearly see. `_tick_held_action` was reading the aim-point material first and bailing on null, so smoothing never ran until a swing had touched a nearby voxel. Fix: let the smooth verb proceed when material is null and use a fallback hold time; `_do_smooth` reads its own cells internally and tolerates unloaded neighbours (treats them as air). | #134
2026-05-05 | VoxelMesherCubes wrote vertex colours but the StandardMaterial3D rendered terrain uniform brown | Default `vertex_color_use_as_albedo = false` on the `StandardMaterial3D` makes it ignore the per-vertex colours the mesher emits. The flat `albedo_color` shows through instead. Set `vertex_color_use_as_albedo = true` AND set `albedo_color = Color(1,1,1,1)` so the vertex colours come through unmodulated. Same pattern lives in `WeatherManager._wet_terrain_material` for the rain-wet override. | #134
2026-05-05 | Setting `vertex_color_use_as_albedo = true` made terrain render nearly black | NOT byte order. `VoxelBuffer.CHANNEL_COLOR` defaults to **8-bit depth** (one byte per voxel), so the 32-bit packed RGBA values the generator writes get truncated to just the R byte — the alpha-byte mat_id alone, ≈0.012-0.016 on screen. Mesher byte order matches Godot's `Color.to_rgba32()` (R high, A low) — no encoding rewrite needed. Fix: instantiate a `VoxelFormat`, call `set_channel_depth(CHANNEL_COLOR, DEPTH_32_BIT)`, assign to `terrain.format` BEFORE handing the terrain to systems that read it. The `format` property is null by default and lives on `VoxelLodTerrain` itself. | #135
2026-05-05 | Calling `out_buffer.set_channel_depth()` inside `_generate_block` made terrain disappear entirely | Confirmed twice in this codebase: the per-block call invalidates the buffer's channel storage even when made on a fresh buffer before any writes. Symptom is "empty chunks, player falls through the world." Always set channel depth globally on the terrain's `format` resource — never per-block. | #135
2026-05-05 | Spent hours rewriting voxel encode + decode across 7 files on a wrong byte-order hypothesis | The actual bug was upstream of byte order (8-bit storage truncating 32-bit values, see #135 above). The mesher byte order matched what `Color.to_rgba32()` produced all along. Lesson: when a render pipeline goes black/garbage, do a single read-back diagnostic FIRST — print one packed voxel from `tool.get_voxel(known_pos)` and look at its hex value. If high bytes are zero, it's storage truncation, not byte order. A sub-second diagnostic would have ruled out the multi-file rewrite. | #135
2026-05-05 | Default `fall_behavior = NEVER` on terrain materials produced rigid-body cluster tumbles for routine digging | `NEVER` routes voxels through `VoxelGravityManager` → spawns `FallingVoxelCluster` rigid bodies that tumble + re-deposit as terrain. Player has to re-mine the rubble. Wrong UX for stone/dirt/grass. Added `FallBehavior.PICKUP_DROP` — unsupported voxels carve and spawn a single `VoxelDrop` (the inventory pickup) at their original position. Reserve `SOLID` for things that should physically tumble (chopped tree limbs the player can chop into logs). | #135
2026-05-05 | FPS counter showed >300 FPS but walking around still felt laggy and "screen-hanging" | The default `Engine.get_frames_per_second()` is smoothed and hides hitches entirely. Track raw delta in a sliding window (60 samples, pre-allocated `PackedFloat32Array`) and surface the worst-frame ms next to the FPS readout — tint red over 33 ms (sub-30 fps spike). Without this, you can't tell whether 300 FPS is actually 300 or "average of 600 FPS and 30 FPS." | #135
2026-05-05 | Stutter ONLY when the player translates, not when the camera rotates | Translation-only stutter means something runs in response to player position change but NOT camera rotation. Greppable callsites: `set_player_chunk`, `set_player_position`, anything that fires on `_player_chunk_changed`. Skip the impulse to blame Zylann's mesh upload first — check the project's own per-position-change handlers. | #135
2026-05-05 | `WaterChunkMesher.set_player_chunk` was iterating `(2*radius+1)^3` chunks per chunk transition | At `MESH_RENDER_RADIUS_M = 64m` and `CHUNK_SIZE_M ≈ 2.67`, that's `49^3 = 117,649` iterations every ~2.7 m of walking. Each iteration does dictionary lookups + AABB tests against every source region. Cost: ~50-150 ms per chunk transition, exactly matching the long-standing "walking stutters but camera rotation is fine" hitch. Fix: water surfaces are 2D planes at fixed Y values. Iterate only the 2D `(dx, dz)` ring at each unique source-region surface Y. With 2 source regions = 49 × 49 × 2 ≈ 4,800 iterations. ~24× faster, fits under 1 ms. General principle: when iterating chunks for an effect that lives on a known plane, never sweep the perpendicular axis. | #135
2026-05-05 | `print()` in voxel hot paths contributed to main-thread hitches | Even though Godot's `print` is thread-safe, the string formatting + ring-buffer sync isn't free. With `_apply_edit` printing per-edit and `[PERF GEN]` summarising every 100 chunks streamed, walking around fired prints constantly. Default every `perf_log_enabled` `@export` to `false`, gate every per-edit / per-frame / per-chunk-load print behind that flag, and reach for `push_warning` (also rate-limited internally) for actual error paths. | #135
2026-05-05 | FPS effectively capped at 60 FPS with no relevant scene setting | V-sync defaults to ENABLED in Godot. Set `window/vsync/vsync_mode = 0` (`VSYNC_DISABLED`) under `[display]` in `project.godot` so the FPS counter is meaningful as a perf signal. Without this you see "60 FPS, smooth" and miss that the game is GPU-limited 30%, idling 70%. | #135
2026-05-06 | Ocean AABB read as flooded inside any tunnel carved below sea level | `_is_water_at_voxel` returned true for any voxel inside the 20×20 km ocean AABB without checking whether terrain blocked the column above. Phase queries had a `_has_clear_vertical_path_to_surface` mitigation but the flow tick bypassed it, so digging into a hill above sea level triggered a gravity-drop flood. Root fix: stop using AABBs entirely. Generator writes per-voxel water bytes into `CHANNEL_DATA` for below-sea-level columns at gen time; tunnels under hills have no water bytes by construction. Bumped `WORLD_GENERATOR_VERSION` 12 → 13. | (Option B)
2026-05-06 | `CHANNEL_DATA` voxels invisible because cube mesher only reads `CHANNEL_COLOR` — useful, not a bug | Confirmed the cube mesher ignores any channel except `CHANNEL_COLOR` regardless of what `_get_used_channels_mask` declares. This is exactly what we want for water: write to `CHANNEL_DATA`, the cube mesher renders the chunk as solid stone where solid (COLOR) and air where air, and `WaterChunkMesher` reads `CHANNEL_DATA` separately to emit transparent water surfaces. No cube-mesher fork needed. | (Option B)
2026-05-06 | Per-voxel `tool.get_voxel` in a tight loop = ~6 s freeze for a 30 k voxel scan | Already burnt by `VoxelGravityManager`'s flood-fill (#135 era). Same pattern bites anywhere that walks a chunk-sized voxel region: water flow tick, water mesher chunk scan, `_chunk_has_any_water` pre-screen. Always pull the chunk's data into a local `VoxelBuffer` once via `tool.copy(origin, buf, channel_mask)`, then iterate the buffer's bytes — one C++ call into Zylann beats N hash-table lookups by 1-2 orders of magnitude. | (Option B)
2026-05-06 | Worker-thread queries to autoloads / physics → silent error or crash | `VoxelGeneratorScript._generate_block` runs on Zylann worker threads. `get_node_or_null("/root/...")` and `PhysicsDirectSpaceState3D.intersect_point` are forbidden there. Pattern for any worker-thread scenery query: cache a snapshot on the main thread (e.g. `NoEditZoneRegistry.get_water_blocking_aabbs_snapshot()` returns `Array[AABB]`), push it into the generator resource via a setter (`generator.set_no_edit_water_aabbs(snapshot)`), then the worker thread reads from a plain local Array. Pure data, race-safe (idempotent reads), no SceneTree access. Document the consequence: runtime-streamed scenery doesn't propagate retroactively. | (Option B)
2026-05-06 | Per-column water mesh quads exploded vert count vs the legacy single plane | A 16×16-column ocean chunk meshed naively as 256 separate quads × 4×4 subdivision = 4096 verts per chunk, ~24× the legacy single-plane vert count for the same coverage. Greedy 2D run-merge collapses contiguous-Y columns into rectangles — a fully-ocean chunk becomes one 16×16 rectangle = ~256 verts. Coastline chunks split into a few rectangles, also bounded. Hard-floor each merged rectangle's subdivision at 2 so the wave shader has interior verts to displace even when the merged rectangle is only ~2.67 m wide. | (Option B)
2026-05-06 | `VoxelBuffer.CHANNEL_DATA` doesn't exist — must use `CHANNEL_DATA5` | Zylann's enum reserves DATA0..4 for TYPE/SDF/COLOR/INDICES/WEIGHTS. `CHANNEL_DATA5` is the first user-defined data channel. Plan referenced `CHANNEL_DATA` throughout — every script using it failed to parse. Substituted `CHANNEL_DATA5` everywhere. Lesson: when planning against a binary GDExtension, dump `ClassDB.class_get_property_list` (and read enum values via reflection) BEFORE writing 6 phases of code that reference an enum value. | (Option B)
2026-05-06 | Water surfaces invisible despite generator + flow + mesher all "working" | Two compounding bugs surfaced together. (1) `(groups[top] as PackedByteArray)[index] = 1` writes to a value-typed Dictionary copy in Godot 4 — explicit read-modify-write is required to mutate the stored array. (2) ArrayMesh surfaces without `ARRAY_NORMAL` render invisible even when the material is `SHADING_MODE_UNSHADED`. Fix: emit normals + UVs alongside vertex positions; use the `var bmp = groups.get(...)`, mutate, `groups[top] = bmp` pattern for any Dictionary<int, PackedByteArray>. | (Option B)
2026-05-06 | Chunked water mesher loaded at the horizon first, crept toward player | Initial fill iterated chunks in row-major order (`for dx in -radius..radius: for dz in -radius..radius`), so the queue filled starting at the corner farthest from the player. Visible water materialised at the horizon and crawled inward over ~10 s. Fix: concentric-ring iteration (`_ring_offsets(r)` returns the (dx,dz) offsets at Chebyshev distance `r`), enqueue r=0..radius. Visible water now appears around the player on frame one. | (Option B)
2026-05-06 | `tool.do_box` silently no-ops with "Area not editable" warning when chunk isn't streamed | Bootstrap test pond at world load + flow tick writes for chunks the player walked toward fast can both hit chunks the streamer hasn't loaded at LOD0 yet. Zylann prints the warning but the write is gone. Fix: `_try_requeue_if_not_editable(tool, voxel_box, cmd)` checks `tool.is_area_editable(box)` first; on false, requeue at the back of `_edit_queue` with a `_retry` counter (cap 60 = ~1 s at 60 fps). Falls back to "assume editable" if the plugin build doesn't expose `is_area_editable`. | (Option B) | (Option B)
2026-05-06 | Water mesh radius 250 m → 35 k MeshInstance3Ds → tens of millions of triangles per frame | Initial Phase 5 set the chunked-mesh render radius to match the terrain view distance so the chunked mesh was the only renderer of water. At 6 vox/m, 250 m = ~189² = 35,721 sea-level chunks → 35 k MeshInstance3Ds in the SceneTree, each with ~256 verts. Drop the chunked radius to 96 m (~1300 instances) and bring back a follow-player horizon plane (one draw call) for distance. CULL_BACK on the horizon plane fixes the original "screen tints blue when below sea level" complaint — the underside is invisible from below. | (Option B)
2026-05-06 | Mesher ate frame budget during initial terrain LOD stream | At a fixed 16 builds/frame, the water mesher consumed ~16 ms/frame even when the terrain streamer needed every spare ms to load LODs. The competition stretched initial load to ~1 minute. Fix: adaptive build budget in `_process` keyed off last frame's delta — 16/frame at <18 ms, ramping linearly to 1/frame at >50 ms. The mesher yields gracefully when the system is under load and ramps back up once frames recover. | (Option B)
2026-05-06 | `VoxelBuffer.is_uniform` is the right pre-screen, not a per-voxel scan | Most chunks at the sea-level row are uniform — either all-air (above-water terrain column) or all-source (open ocean). `buf.is_uniform(channel)` is O(1) inside the plugin. Wrapping the per-column scan in an is_uniform check turns the dominant case from 4096 `get_voxel` calls into 1 plugin call + a single byte read for the canonical value. Use this anywhere a chunk-sized voxel scan asks "does this chunk have any X?" before the expensive walk. | (Option B)
2026-05-06 | Loading screen ran at <10 FPS / 150 ms worst-frame on `gl_compatibility` while chunks streamed behind the curtain | The actual culprit was **`HUDOverlay` + `JournalUI` CanvasLayers were still issuing draw calls every frame** even though they were visually covered by the layer-100 loading overlay. `HUDOverlay`'s compass alone calls `queue_redraw()` + multiple `draw_string` (font glyph rasterisation) per frame, which is real work on `gl_compatibility`'s single-threaded renderer. Fix in `TransitionManager._show_loading_screen`: **set `visible = false` on `HUDOverlay` and `JournalUI` CanvasLayers** so Godot stops issuing draw calls for them — `_process` keeps running, but `_draw` doesn't fire. Pair with `viewport.disable_3d = true` for any extra rasterisation savings. Restore both in `_hide_loading_screen` from a `_hidden_canvases` list of {node, prev_visible} so JournalUI mid-fade state is preserved. Result: load screen runs at full UI frame rate. **Importantly do NOT pause `VoxelLodTerrain` / heavy autoloads / the destination scene root during the loading hold** — an earlier iteration did, which fixed the FPS but defeated the whole point of the loading screen (give Zylann's worker threads + main-thread upload time to load chunks before the player sees the world). The Zylann main-thread polling + chunk-mesh upload work is precisely what the hold exists for. Lesson: invisible-but-still-drawing canvases are easy to miss when chasing perf, and the right answer is suppressing the LOAD-SCREEN's own waste, not the destination scene's productive work. | (Option B)
2026-05-07 | `VoxelLodTerrain.collision_update_delay` is INT in this Zylann build, not float | `terrain.set("collision_update_delay", 0.1)` silently truncates to int 0 — every chunk streaming in/out then fires immediate main-thread collision rebuilds, producing 100+ ms spikes during movement. The `set()` call doesn't error; the property dump and read-back are how you catch it. Pass integer milliseconds: `terrain.set("collision_update_delay", 100)` and read back the value to confirm. Same gotcha may apply to other Zylann int-typed properties — read-back is the only safe verification. | (perf 2026-05-07)
2026-05-07 | World3D.tscn missing `cache_generated_blocks = true` — generator re-ran on every chunk eviction | `cache_generated_blocks` defaults to false on `VoxelLodTerrain`. `save_generator_output = true` on the stream isn't enough — both flags must be set or generator output is thrown away when chunks unload. Already documented for the bake pipeline (COPPER_ISLES_BAKE_NOTES.md 2026-05-06 entry); World3D was missing it because the flag was only added to BakeWorld + CopperIslesTest at the time. Symptom: generator running on every fresh exploration AND every backtrack through previously-loaded terrain. Fix: add `cache_generated_blocks = true` to the VoxelLodTerrain block in any scene that uses on-the-fly generation. | (perf 2026-05-07)
2026-05-07 | `streaming_system = 1` (CLIPBOX) broke streaming for World3D | CLIPBOX treats `terrain.view_distance` as a strict cap. World3D has the default 512-vox view_distance (~85 m world at 1/6 scale); player spawns at Y=120 while heightmap peaks at Y=33 → closest terrain is ~87 m below, JUST outside the cap. Result: zero terrain streamed, player free-fell into the void, Zylann's worker threads burned CPU chasing out-of-bounds chunks. Legacy octree (`streaming_system = 0`) is more lenient. CopperIslesTest can use CLIPBOX because the SQLite is pre-baked AND the player spawns inside the bake area. Re-evaluate CLIPBOX for World3D when (a) `terrain.view_distance` is bumped, or (b) the spawn moves closer to ground. | (perf 2026-05-07)
2026-05-07 | `WaterChunkMesher` consumed 700-1200 ms/sec of main-thread time during walking | Per-autoload profiler (`HUDOverlay.profile_record` bucket) attributed almost the entire frame budget to the water mesher. Two compounding causes: (1) `MESH_RENDER_RADIUS_M = 96` produced ~5300 candidate chunks at the sea-level row, with ~73 newly entered per chunk crossing; (2) `_gather_surface_quads` called `tool.copy()` on every dirtied chunk, which BLOCKS for 5+ ms when the chunk hasn't been streamed by Zylann yet. The mesher also competed with Zylann's main-thread mesh-apply queue, which is why terrain failed to render while the mesher was working. Three-part fix: cut `MESH_RENDER_RADIUS_M` to 32 m (~9× fewer candidates), drop `MESH_BUILDS_PER_FRAME_MAX` from 16 to 6, and add a `tool.is_area_editable` (~1 µs) probe before `tool.copy` so unloaded chunks bail fast. Result: mesher dropped from ~900 ms/sec to ~14 ms/sec; walking FPS went from 38 (frame-budget-locked) to 200+. | (perf 2026-05-07)
2026-05-07 | `WaterChunkMesher.set_player_chunk` re-iterated the full ~5,300-chunk square on every chunk crossing | Same scaling issue as the 2026-05-05 entry above (different scope). At fly speed (~19 chunk crossings/sec) that was ~100k dict ops/sec synchronously inside `_physics_process`. Replaced the full-ring iteration with a delta-only path: only the strips of chunks that newly entered the radius get marked dirty. ~73 chunks for an axis-aligned move, ~145 for a diagonal. First call (no prior chunk) and large jumps fall back to the ring-ordered full-fill so visible water still materialises around the player on initial load. General principle: when a "track this around the player" routine has an O(radius²) inner loop, look for the delta-only formulation first. | (perf 2026-05-07)
2026-05-07 | `Performance.TIME_PROCESS` / `TIME_PHYSICS_PROCESS` are NOT script attribution | These monitors are per-frame snapshot timings — useful as sanity checks (they track close to `worst_ms` during spikes) but they don't isolate which script is slow. Wasted a debug round assuming proc=200 ms meant 200 ms of GDScript work. The right approach for "which autoload is eating frame time" is wrapping each `_process` / `_physics_process` with `Time.get_ticks_usec()` and accumulating to a per-second bucket. `HUDOverlay.profile_record(label, usec)` is the project convention; the `[PERF] top: ...` line dumps the top 3 contributors every second. Wrappers live in `WaterChunkMesher`, `WaterFlowManager`, `WeatherManager`, `DayNightCycle`, `Player3D._physics_process_inner`. | (perf 2026-05-07)
2026-05-07 | `[FRAME SPIKE]` per-frame print became a feedback loop at sub-10 FPS | The diagnostic in `HUDOverlay._process` printed every frame > 50 ms. At sub-10 FPS, hundreds of prints/sec hit Godot's Output panel — and each print costs 0.5–2 ms when the buffer is large. The print itself was contributing to the spike it was measuring. Replaced with a 1-second aggregated `[PERF]` summary that emits once per window, gated by a `PERF_DIAG: bool = true` const. General rule: any per-frame diagnostic print needs a rate limiter, otherwise diagnosing perf hurts perf. | (perf 2026-05-07)
2026-05-07 | `mesh_block_size = 32` (vs default 16) cuts chunk count ~8× without touching `lod_count` or `view_distance` | When `lod_count` and `view_distance` are off-limits (e.g. shared with another scene's bake), `mesh_block_size` is the only knob that meaningfully reduces chunk count for a given view volume. Each mesh chunk covers 32³ voxels instead of 16³, so the same view volume divides into ~8× fewer chunks. Trade-offs: (a) edits become slightly more expensive (one mining swing rebuilds a 32³ mesh block instead of 16³); (b) coarser LOD pop boundary at chunk seams; (c) higher VRAM per chunk (more verts) offset by 8× fewer meshes net. Verify Zylann accepted the value via property read-back — some builds clamp to 16. | (perf 2026-05-07)
2026-05-09 | **Built every new UI without manual `_input` dispatch and burned 8+ commits debugging "broken" buttons** | Building `DiceGameUI` from scratch we used the standard Godot pattern: `Button.pressed.connect(...)`, `HSlider.value_changed.connect(...)`, expecting Godot's GUI input chain to deliver clicks. **It silently doesn't.** Spent a long debug session adding `_input` traces, `gui_input` listeners on every container, layout dumps, viewport-size diagnostics, even converting the modal root from `CanvasLayer` to `Control` and back — none of it fixed clicks because the events never enter the GUI input pipeline at all. The fix was finding `PauseMenu.gd:452-462`'s comment, which already documents this: *"GUI dispatch is silently disabled in this project (likely Dialogic's input subsystem listening to LMB events globally interfering)."* Lesson #25 (2026-05-03) is the same lesson — re-learning it cost a full session. **Codify this for every future UI:** any new menu, modal, mini-game UI, settings panel, etc. in this project must follow the manual-dispatch pattern from day one. The pattern is: implement `_input(event)` on the UI root, filter `InputEventMouseButton.pressed` + LMB, walk visible+enabled controls in `_dispatch_click(pos)`, hit-test each via `ctrl.get_global_rect().has_point(pos)`, fire its handler manually. For sliders, translate click X to value: `t = clampf((pos.x - rect.x)/rect.w, 0, 1); slider.value = min + t*(max-min)`. **Do not** spend any time debugging why `Button.pressed` isn't firing — that ship sailed in May 2026; the answer is always "implement manual dispatch." Until/unless the Dialogic root cause is fixed, every UI in this codebase follows this convention, and CLAUDE.md should reference it under critical patterns. Reference implementations: `PauseMenu._input` / `_dispatch_click` / `_hits` (canonical), `MainMenu`, `Settings`, `DiceGameUI`. | feat/dice-art-and-audio-wiring
2026-05-09 | Gemini Nano Banana 2 rejected texture-prompt blocks with a recitation filter | The filter trips when one prompt block stacks too many specific signals at once: hex codes + named genre ("voxel game block face texture for a medieval fantasy RPG") + heavy negation language ("no baked lighting, no cast shadows...") + imperative voice ("Generate a..."). It reads as "specific reproduction request" and refuses. **Fix**: split into two parts — a verbose SYSTEM PROMPT (paste once into the system/style/context field) carrying meta + tiling math + style language, and short pure-descriptive per-material prompts (paste into the main prompt field). The split distributes the signals across fields so neither alone trips the filter. **Also**: drop ` --tile` (Midjourney-only — Gemini parses as malformed), pixel dimensions belong in UI settings not in the prompt, replace em-dashes / `×` with ASCII equivalents. See `tools/AI_TEXTURE_PROMPTS.md`. | tileset-KJNfH
2026-05-09 | At 6 vox/m a "subtly distinctive" texture became a visible repeating grid | Each voxel face is ~16.7 cm; a 10 m wall repeats the tile 60+ times. Any single bright pebble, prominent vein, off-color patch, or asymmetric crack becomes an obvious gridded pattern the player can't unsee. Prompts must explicitly forbid dominant features and ask for **uniform fine-scale variation** — many small things of similar visual weight, not one distinctive thing. The first stone texture had cobblestone-shaped chunks that locked the eye on every wall; the second pass with "fine micro-fractures of varying short length distributed evenly" tiled invisibly. Lesson: bake the repeat-count math into prompt language, not just the post-gen QA pass. | tileset-KJNfH
2026-05-09 | AI image generators don't actually output RGBA even when prompts ask for transparency | Gemini Nano Banana 2 (and DALL-E and Midjourney in their default modes) emit flat RGB on a colored background — leaves came back as "deep emerald clusters on solid white" rather than alpha-cutout. **Fix**: ask the prompt for a pure white background between gaps, then color-key white to alpha during atlas build (`tools/build_texture_atlas.py` → `chroma_key_white()` using min-channel detection so saturated greens don't get caught). Min-channel handles bright lime correctly where luminance-based keying would chew it. Future cutout materials (other foliage, fabric, glass) get added to `CHROMA_KEY_MATERIALS` and pick up the same treatment. | tileset-KJNfH
2026-05-09 | Atlas builder produced atlas.png at 1024 px per tile because AI generators ignored the prompt's pixel size | Gemini outputs 1024×1024 by default regardless of `512×512` in the prompt; the latter belongs in the API's image-size setting, not the prompt. The downscale step in `build_texture_atlas.py` (`Image.resize(..., LANCZOS)`) handles any source size correctly — keeping larger sources actually antialiases the 32×32 result more cleanly. Lesson: prompt-text dimensions are advisory at best; rely on the post-process resize as the ground truth. | tileset-KJNfH
2026-05-09 | Zylann blocky terrain rendered all white despite a fully-populated atlas + library .tres | The atlas, the per-cube tile coords, the per-cube material, and the library bake all looked correct in `blocky_library.tres` (file inspection confirmed `material_override_0 = SubResource(...)` and `tile_top = Vector2i(2, 0)` etc.). But at runtime, `model.get_material_override(0)` returned null and faces sampled UVs across the full 2048×2048 atlas (not just one 32×32 tile). **Root cause**: Zylann's gdextension exposes `material_override_0`, `tile_top`, `tile_bottom`, etc. via `_get_property_list` as DYNAMIC PER-SURFACE / PER-METHOD properties. `ResourceSaver` writes them to .tres (round-trip looks fine on disk), but `ResourceLoader` doesn't restore them — fresh model instances come back with defaults. **Fix**: re-apply tiles + materials at runtime in `World3DBootstrap._inject_atlas_materials_into_library()`. Walk the library's models, call `set_tile(SIDE_*, vec)` and `set_material_override(0, mat)` on each non-null cube, then `lib.bake()`. The single source of truth is the runtime injection, not the .tres. | tileset-KJNfH
2026-05-09 | `VoxelLodTerrain.material` silently overrode every per-cube `material_override_0` to render an empty `StandardMaterial3D` | World3D.tscn carried a leftover stub `VoxelTerrainMat` (just `roughness = 0.85`, no albedo texture) on the terrain node. In Zylann blocky meshing, `terrain.material` overrides the per-model materials project-wide — so faces ignored their atlas materials and rendered with no albedo (solid white). **Fix**: leave `terrain.material` null when using textured cube models with per-cube `material_override_0`. The terrain.material slot is for "I want one shader on every face" cases; for atlas-textured terrain, per-cube is the path. | tileset-KJNfH
2026-05-09 | `.set("tile_top", Vector2i(2,0))` and `.set("material_override_0", mat)` silently no-op'd despite the property appearing in `get_property_list()` | The first build_blocky_library.gd guessed at API names AND used `.set()` for everything. Even the saved-but-correct property names didn't stick because Zylann gdextension routes those properties through method pairs (`set_tile(side, vec)` / `get_tile(side)`, `set_material_override(idx, mat)` / `get_material_override(idx)`). `.set()` writes to a virtual property bag that the gdextension reads from for serialization but not for the actual mesh build. **Fix**: always use `.call("set_tile", SIDE_*, vec)` and `.call("set_material_override", 0, mat)` for these. Plain properties (`atlas_size_in_tiles`, `transparency_index`, `culls_neighbors`) work via `.set()` fine. **General rule**: when a property has a corresponding `set_X` method exposed in `get_method_list()`, prefer the method. **Diagnostic**: write a probe EditorScript that prints the full `get_property_list()` and `get_method_list()` for any plugin class before committing to property names — the property name list alone misleads. See `tools/probe_zylann_blocky.gd`. | tileset-KJNfH
2026-05-11 | godot-cpp can't subclass `VoxelGeneratorScript` directly — the auto-wrapper only covers engine-core classes, not third-party extensions like Zylann's | Tried subclassing Zylann's `VoxelGeneratorScript` from C++ and the build can't find a header to extend. **Fix**: C++ class extends `godot::Resource`, and a thin GDScript adapter extends `VoxelGeneratorScript` + forwards `_generate_block` (and any setters the bootstrap calls) into the C++ Resource via Variant. The adapter is the node-tree-visible generator; the C++ Resource does the work on Zylann's worker threads. Mirror this for every future C++ port that needs to plug into a Zylann hook. See `extensions/voxel_gen/` for the canonical implementation. | feat/cpp-generator-port
2026-05-11 | `"property_name" in generator` returns false even when the property lives on the C++ Resource exposed as `generator.cpp_impl` | When the C++ adapter wraps a `CubicHeightmapGeneratorCpp` SubResource via `@export cpp_impl: ...`, lookups on the adapter (`"sea_level_voxels" in generator`) miss the C++ property because it's a hop away. `WorldBakeController._classify_tiles` silently defaulted to `sea_level_voxels = 1200` → every World3D tile classified DEEP_OCEAN → bake walked 0 tiles. **Fix**: any duck-typed reflection on a generator that may be the adapter must drill one level: check `"cpp_impl" in generator` and read from `generator.get("cpp_impl").get(prop)` as the fallback. General rule for adapter-wrapped Resources: every public lookup site has to know about the cpp_impl hop. | feat/cpp-generator-port
2026-05-11 | The bake's `WAIT_PER_POSITION_S = 6.0` was a hardcoded safety margin tuned for the slow GD EXR-sampling generator — pure overhead after the C++ port | Bake estimate exploded to 3 hours for a 1km World3D bake because the controller waits 6s at every walker stop regardless of how fast workers complete their chunks. The wait dominated wall-clock by 2 orders of magnitude vs. real generator time. **Fix**: promote the constant to `@export var wait_per_position_s` so each bake scene tunes its own value (1s on World3D C++ chain, 6s preserved on Copper Isles GD chain). General pattern when porting any hot loop: scan the calling code for hardcoded "be patient" sleeps that were tuned around the old slow path; those become the new bottleneck unless they're moved to data. | feat/cpp-generator-port
2026-05-11 | Bake skipping LOD 0 isn't enough — the viewer's `view_distance = 8000 vox` still streams the entire LOD pyramid above the sky and below the world floor | Each walker stop spent the bulk of its work on chunks the player will never see: empty air at +500 m, deep stone at -200 m. The fix that's transferable to any bake: clip `terrain.voxel_bounds` Y range to a sane band before walking, restore on completion. Chunks outside the band don't stream → generator never runs on them. Two `@export`s on the bake controller (`bake_y_min_voxels`, `bake_y_max_voxels`) keep it tunable per scene. Defaults wide enough not to clip Copper Isles' 15000-vox peaks. | feat/cpp-generator-port
2026-05-11 | "GDScript float == GDScript float" survives a port to C++ only when both sides use double, not single | Bit-exact parity on `_ground_y_at` required treating `FastNoiseLite.get_noise_2d()`'s float-32 return as `double` immediately on the C++ side (Godot Variant float is double). Mixed single/double promotion in late accumulation produces a 1-ULP delta that flips an int truncation 0.01 % of the time. The parity harness caught it — `int(double)` truncates toward zero in both languages; `roundi(double)` → `std::lround(double)` matches. Lesson for any port of GD math: store noise samples as `double` immediately on entry to your C++ math, even when the source library returns `float`. | feat/cpp-generator-port
2026-05-12 | C++ port of `CopperIslesHeightmapGenerator` rendered chunky out-of-place voxel blocks at distance — bit-exact parity test (262 k voxels across LOD 0-7) passed | Latent thread-safety race in the lazy `_ensure_image()` heightmap cache. Zylann calls `_generate_block` from many worker threads concurrently; the GDScript original's `if not _heightmap_load_attempted: _load_exr(); _heightmap_load_attempted = true` check-then-set was racy in both languages, but the slower interpreter rarely lost the race. The C++ port hits it reliably — one thread passes the check before another finishes populating `_heightmap_w`/`_heightmap_h`/`_heightmap_image`, reads stale or zero state, returns gray=0.0 → ground_y=0 → terrain emitted at world Y=0 instead of the actual ~Y=1500 (sea level) → "floating cubes" artifact in the LOD pyramid. **Fix**: wrap `_ensure_image()` in `std::mutex`, and set `_heightmap_load_attempted = true` LAST after `_heightmap_w`/`_heightmap_h`/`_heightmap_image` are fully populated so observers either see the complete cache or take the lock to populate it. General rule for any C++ port of a GDScript lazy-init pattern: GDScript's GIL-like interpreter lock masks races that surface immediately under C++. Add a mutex to every cache-on-first-use path crossed by worker threads, and order writes so the "done" flag flips last. Debugging path: bit-exact parity harness ruled out generator logic; `_get_used_channels_mask` diagnostics confirmed Zylann ignores the override on adapter-wrapped generators (innocuous — `terrain.format` allocates channels anyway); the smoking gun was the GDScript working, the C++ port failing, despite identical logic — pointing at the language-specific concurrency floor. | feat/cpp-generator-port
2026-05-12 | World3D `F6` fresh launch: player spawned at scene's Y=120 and fell forever (or 100m+ before catching) despite a 5-25s spawn-freeze + raycast retry | Zylann's CLIPBOX streaming_system (and likely the legacy octree) only re-evaluates the chunk set when the **viewer's transform changes**. A frozen viewer (`_spawn_freeze=true` gating `_physics_process_inner`) is perfectly stationary → Zylann never builds the chunks under the player → no collision → raycast never hits → freeze times out → fall. Log evidence: `time_detect_required_blocks=30-100µs` (Zylann polling but doing nothing), `time_mesh_requests=0` (no chunks meshing), `dropped_block_loads=0`. **Fix** (`World3DBootstrap` + `CopperIslesTestBootstrap`): per-physics-frame WIGGLE — nudge `player.global_position.x` by ±1mm every 6 frames (~10 Hz at 60Hz physics) while the freeze is active. Microscopic but enough position delta to keep Zylann's detect loop emitting chunk requests. Combined with an analytical pre-snap (using `generator.get_ground_voxel_y_at()` to place the player ~3m above actual ground before freeze, so the LOD0 sphere covers the ground from frame one), spawn typically clears in 5-12 s. Also flag the failure mode in the reverse direction (next entry below). | feat/profiler
2026-05-12 | Spawn wiggle tuned 1mm→10mm and single-axis→X+Z broke Zylann: `time_detect_required_blocks=208,863µs` (12× frame budget), `dropped_block_loads=16,714`/sec, FPS 11-14, player fell anyway | At 10mm × dual-axis × 60Hz, the wiggle invalidated Zylann's chunk box FASTER than Zylann could process each batch. Every frame: wiggle moves viewer ~14mm diagonally → Zylann queues a fresh chunk batch → next frame's wiggle invalidates the box → batch canceled → repeat. Net: thousands of dropped loads per second, no batch ever completed, no collision ever built. **Proven config** is the opposite of "more aggressive": 1mm × single-axis × every 6 frames (10Hz). The interval gate is the key — Zylann needs breathing room BETWEEN nudges to actually finish each batch. Raycast still runs every frame (cheap). General rule for invalidate-based streamers: position-change frequency must be LOWER than the streamer's processing rate, or the queue thrashes. Pulse, don't oscillate. | feat/profiler
2026-05-18 | New autoload (`AudioManager`) with Node-typed vars crashed every autoload at parse time | Building `AudioManager` we wrote `var p: Node = ...; p.bus = "SFX"; p.stream = s; p.play()`. GDScript 4 **static typing rejects unknown PROPERTY access on a typed base** (`Node` has no `bus`/`stream`) at parse time — and a parse error in an autoload aborts the *entire* autoload chain, not just that script, so the game booted to a wall of red with no player. Method calls on typed vars (`wfm.is_position_in_water()`) only warn (UNSAFE_METHOD_ACCESS) and run; **properties are the hard error**. Fix: set `.bus`/`.stream` on a concrete-typed local (`var p2 := AudioStreamPlayer.new()`), keep the cross-type handle in an *untyped* var, and use `.set("loop", true)` for subclass-only members. General rule: any node whose concrete subtype varies must be built on the concrete type and stored untyped — never reach through a `: Node`-typed var for a property. Codified in CLAUDE.md critical patterns. | claude/lotr-music-prompts
2026-05-18 | Footsteps stayed silent in-game even though wiring + files were correct; then "machine-gunned" one clip | Two compounding causes. (1) `AudioManager._resolve` **cached the miss**: it ran before Godot finished importing freshly bulk-copied `.mp3`s, cached an empty result for each id, and never re-probed for the whole session. Fix: cache HITs only; a miss re-probes next trigger (cheap at trigger rates) so sounds switch on the instant the import lands. (2) `is_on_floor()` flickers for a frame or two while Zylann streams collision under a moving player, which zeroed the footstep distance accumulator every flicker → no plant ever accumulated. Fix: coyote grace — only reset the accumulator after >0.25 s genuinely airborne. General rule for no-op-safe asset resolvers: never cache the "absent" answer when assets can appear mid-session; and never trust a single-frame `is_on_floor()` on streaming terrain — debounce it. | claude/lotr-music-prompts
2026-05-18 | Panicked over a PR that "deleted" the whole Water V2 milestone — the two-dot `git diff main..HEAD` lied | A long-lived branch (cut 8 commits before Water Voxel V2 squash-merged to `main`) showed, via `git diff origin/main..HEAD`, that it would delete `WaterDiag.gd`, gut `WaterFlowManager.gd` (−924), resurrect `WaterChunkMesher.gd` (+1049) and remove the V2 plan docs. Those deltas were **`main` advancing, not the branch changing anything** — two-dot diff shows all divergence including the base's progress. The merge GitHub actually performs is the **three-dot / merge-base diff** (`git diff origin/main...HEAD`), which was pristine (purely additive audio files, `MERGEABLE`/`CLEAN`). Lesson: before trusting a scary pre-merge diff on a branch that's behind `main`, check `git merge-base`, the behind/ahead counts, and the **three-dot** diff — that is what merges. Don't rebase/"fix" a branch off a misread two-dot diff. | claude/lotr-music-prompts
2026-05-18 | ElevenLabs SFX credit burn calibrated only after real spend — placeholder cost model was ~20× off | `render_sfx.py` shipped with a guessed `$0.08/clip` flat model; first real batches (Water 1,697 cr, Cat 04 ~3,000 cr) showed the true cost is **≈8.2 credits/sec + ≈19 cr/generation floor** (the per-gen floor dominates for short SFX, which the flat model ignored entirely). An apparent "1.9× locomotion overrun" was a *mis-attribution* of multi-batch cumulative spend to one batch, not a real overrun. Fixes: model as `max(floor, per_sec*dur)` with a slightly conservative 9 cr/s & 20 cr/gen (estimates run ~6 % high so the credit cap stays protective), and always reconcile spend against the account's own meter, not the script's running tally, before trusting an "overrun." General rule for any metered-API batch tool: do not trust the cost model until it's been reconciled against two independent real bills; build the hard credit-cap kill switch in from commit 1. | claude/lotr-music-prompts
2026-05-18 | Bulk-placing 548 raw SFX takes: per-file id-prefix mapping was mandatory, folder-name mapping would have mis-filed dozens | The ElevenLabs review folders (`09_fire_camp/` etc.) don't map 1:1 to repo semantic folders — `09_fire_camp` holds both `fire_*` (→`environment`) and `camp_rest_*` (→`ui`), and `water_wade`/`water_entry` (→`locomotion`) must beat the more general `water_` (→`environment`). The only correct placement is per-file, matching the **exact ordered, longest-prefix-first** list `AudioManager.PREFIX_FOLDER` resolves with (mirrored verbatim in the placement script). Mapping by review-folder name would have silently mis-filed the `camp_rest_` chimes and any `water_wade*` so `AudioManager._resolve` could never find them. General rule: when two systems must agree on a routing table, don't paraphrase it on one side — copy the ordered list verbatim and note the dependency. | claude/lotr-music-prompts
2026-05-19 | `water.gdshader` failed to compile in-engine after PR #232 C1 ("Using `return` in the `fragment` processor function is incorrect"); water rendered all-white | Godot 4 spatial shaders DISALLOW `return` inside `fragment()` (and other processor functions). Used `return;` to early-exit after the back-face branch wrote its output — illegal. Fix: introduce a `bool _uw_back_face` flag and wrap the rest of `fragment()` in `if (!_uw_back_face) { ... }`. Functionally identical, compiles clean. Headless missed it because `--headless` uses the dummy renderer and never compiles shaders (CLAUDE.md "headless = data/logic/parity only, visuals are GPU-only"). Rule going forward: any shader change must designer-verify in the editor before merge — headless RESULT=PASS is necessary but not sufficient for shader work. | #232 (hotfix 32a8109)
2026-05-26 | "Player outruns LOD0 after 10 s of sprinting" — chased the wrong root cause for an entire session before finding it was an off-by-6 UNIT BUG buried in a comment | `Player3D.VIEWER_LOOKAHEAD_MAX_OFFSET_M = 40.0` had a comment claiming "40 m is ~31 % of the 128 m LOD0 ring." But `terrain.lod_distance = 128` is in VOXELS, not metres — at the project's 6 vox/m scale the LOD0 ring is ~21 m, so 40 m was pushing the main viewer 188 % of the ring radius AHEAD of the player. The player was standing OUTSIDE their own LOD0 ring whenever they moved at any non-trivial speed. Fix: 40.0 → 0.0 (already supported as the kill-switch). General rule: when chasing a perf/streaming bug, dump and unit-check every related constant — physical interpretation in the comment is not authoritative. The fastest path to the truth was `[DIAG] viewer=(...) lag_xz=23.8 m` showing the main viewer 23 m ahead of the player; that one line ended a 6-hour rabbit hole. | #240
2026-05-26 | Multi-viewer "prefetch train" of 3 secondary VoxelViewers spaced ahead of the player turned out to be an anti-feature, NOT the LOD-corridor extender it was designed as | Four viewers (main + 3 train) each issuing independent LOD0 chunk-load requests at sprint speed thrashed Zylann's clipbox scheduler — the train viewers continuously shifted ahead, canceling each other's in-flight loads and re-requesting at new positions. Capture before disable: `zylann.detect_us` p99 = 465 ms (main-thread frozen for half a second), `dropped_block_loads` peak 30,000+/s. After deleting the train: 14 ms p99, zero dropped loads. General rule: in Zylann's CLIPBOX streaming, more viewers ≠ more LOD0 coverage — the scheduler's mesh-block ref-counting means N viewers requesting overlapping regions just multiply the cancel-restart pressure. One viewer glued to the player, no offset, is the correct configuration. The "prefetch train" pattern is documented as deleted in `scripts/Player3D.gd` lookahead comments. | #240
2026-05-26 | F11 LOD band debug shader updated only on toggle, not live during gameplay — `set_shader_parameter` writes silently failed to propagate | First attempt used a per-material uniform: `var mat := ShaderMaterial.new(); mat.shader = ...; terrain.material = mat;` then `mat.set_shader_parameter("player_world_pos", pos)` each frame. The colour bands locked at the position F11 was first pressed and never updated. Root cause: **Zylann's `VoxelLodTerrain` duplicates `terrain.material` per chunk** under the hood — `terrain.material = mat` clones `mat` to each MeshInstance3D, so our cached reference points to a "donor" the GPU never renders from. Re-toggling F11 reassigns `terrain.material` which forces a fresh duplication, picking up the latest uniform — hence "only updates on toggle." Fix: register a GLOBAL shader parameter via `RenderingServer.global_shader_parameter_add(...)` and write to it via `global_shader_parameter_set(...)`. The shader reads it as `global uniform vec3 player_world_pos`. Globals are pushed once and applied to every shader that references them — no per-material instancing to fight. General rule for any per-frame value that needs to reach a Zylann-rendered shader: use a `RenderingServer` global shader parameter, never `set_shader_parameter` on a `ShaderMaterial` you pass into `terrain.material`. | #240
2026-05-26 | LOD1+ water-surface "transparent line" artefact misdiagnosed for half a session as mesh gaps showing the sky behind partial-fill water voxels — a backdrop horizon plane "fix" did nothing but introduced an underwater dark band regression | Symptom: thin transparent horizontal lines at chunk boundaries on lake surfaces past the LOD0 ring, gone at LOD0. First hypothesis: Zylann's `VoxelBlockyModelFluid` mesher produces tiny vertical gaps between adjacent LOD0 and LOD1 chunk faces because the fluid auto-slope only sees within-chunk neighbours. "Fix": a 2 km × 2 km dark-blue plane just below water surface, `cull_back`, follows the player. Plane was visually IDENTICAL behind the lines, proving the lines were NOT showing sky — they were being PRODUCED by the water shader at chunk seams. (Toggling F6 shader debug modes confirmed: lines blended away in mode 5 flow-vector, stayed visible in modes 0/1/2/3/4/6 — the common factor is the reflection path, not the simulation data.) Actual root cause: **reflection-normal discontinuity** in `water.gdshader` where adjacent LOD0/LOD1 chunks have slightly different normal computation, making the reflection vector flip toward the sky at the seam. Plane reverted; lines deferred to a dedicated water-shader session. General rule: when a "transparent" line on a transparent surface persists with a known-opaque backdrop behind it, the artefact is in the surface shader's output, not in the geometry; debug modes that bypass the reflection / fresnel / depth path are the fastest localiser. | #240 (deferred follow-up)
2026-05-26 | Two distinct cluster outputs (loose landings + cluster groups) were tempted to return as `Dictionary[Vector3i, int]` from the VGM C++ port — would have paid per-Variant marshalling on every voxel of every cluster | A 4096-voxel cluster (the per-bubble cap) returned as a Dictionary keyed by Vector3i would marshal ~4096 Variant pairs across the boundary on every spike-triggering blast — undoing a large fraction of the perf win the port exists for. Fix: chose `PackedInt32Array` stream returns instead (`[x, y, z, packed, ...]` flat per cluster, segmented by a separate `cluster_counts` array). Same parity surface (the harness still compares semantically), ~5× cheaper marshalling, and no coupling between C++ and the GD-side write-Dict convention (`{"pos", "value"}`). General rule: when a C++ port has to return per-voxel data to GD, default to packed-array streams — Dictionary marshalling at scale is invisible-but-real overhead the per-byte cost analysis above the call site misses. | #241
2026-05-26 | Reused `sort_custom(func(a,b): return a.y < b.y)` as the GD reference's loose-voxel sort key — but `Array.sort_custom` is UNSTABLE, so two voxels at the same Y could come out in either order and trip the parity harness against `std::sort` non-deterministically | The VGM autoload's existing `_handle_loose_voxels` had a Y-only sort and got away with it because voxels at the same Y are in different (x, z) columns and don't interact during column-fall. But for parity vs C++ `std::sort` (which is also unstable but uses different internal ordering), any equal-Y voxels could land differently in the two implementations and falsely fail the harness. Fix: both sides sort by full (y, x, z) lex tuple — fully deterministic, behaviour-equivalent for the column-fall logic. General rule for any GD↔C++ parity harness: don't depend on either language's unstable sort agreeing — pick a total ordering and use it on both sides. | #241
2026-05-27 | Assumed Zylann `VoxelBuffer.get_channel_as_byte_array(CHANNEL_TYPE)` returned bytes in X-fastest order (`byte = x + y*sx + z*sx*sy`) when porting the baked-light reads. The wall-bleed-through parity probe (red emitter west, blue east, stone wall between) returned 16 lit cells AT the wall column, breaking the gate. Wasted ~20 min trying to chase the wrong root cause | The actual layout is **Y-FASTEST**: `byte_index = (y + x*sy + z*sx*sy) * bytes_per_voxel`. Confirmed by a marker-voxel probe — set distinct values at `(1,0,0)`/`(0,1,0)`/`(0,0,1)`, check the byte indices. Also: `get_channel_as_byte_array` returns 2 bytes per voxel for Zylann's default 16-bit channels, 1 byte for the 8-bit project setup. Derive `bytes_per_voxel = byte_count / voxel_count` so the same code handles both; read the LOW byte at the voxel's offset (mat_ids < 256). General rule: any time you bulk-read a Zylann channel from GD or C++, use the Y-fastest formula and derive bpv from the size — don't hardcode either. | #241
2026-05-27 | C++ port boundary that uses per-voxel `p_buf.call("get_voxel", ...)` Variant calls (the pattern from `voxel_gravity_cpp.cpp` and `emissive_light_cpp.cpp`) is MEASURABLY SLOWER than native GDScript per-cell access when the loop iterates more than a few thousand cells | Each `p_buf.call("get_voxel", ...)` from C++ goes through Variant's general dispatch + arg boxing — measurably more expensive than GDScript's typed VoxelBuffer.get_voxel which Godot dispatches natively. This is why ELM's C++ scan_region regressed from 18 ms (GD-native) to 47.6 ms (C++ via Variant) on the per-voxel path. **Fix:** one bulk `p_buf.call("get_channel_as_byte_array", channel)` returning a PackedByteArray, then iterate bytes natively in C++. One Variant crossing instead of N^3. Applied to `emissive_baked_cpp.cpp` (Phase J) and `water_flow_cpp.cpp` (settle scan) from the first commit; pattern available to retrofit any earlier port that's hot enough to matter. General rule: a C++ port that reads voxels per-cell via `Variant::call` saves NOTHING vs GDScript — the win only appears when the C++ side amortises one bulk read across the whole loop. | #241
2026-05-27 | Voxel texture atlas builder was designed for an AI workflow that turned out to be wishful thinking — general-purpose diffusion models (Nano Banana / DALL-E / Midjourney / SD-default) cannot reliably output pixel-art-on-grid even with strict prompt discipline | The pipeline does NEAREST 1024 -> 16 downscale, which assumes each 64x64 source region is a flat block of one colour. On photo / painterly input, NEAREST samples one essentially-arbitrary pixel per region and produces 16 unrelated pixels = visual noise. Designer flagged copper_ore in-game looking nothing like its source PNG; turned out the source was 1024x1024 photo-style (continuous gradients, no pixel grid), so NEAREST output WAS noise — and 5 of the 15 current sources have the same problem. **Fix (rejected):** switch to LANCZOS downscale — would produce blurry-but-recognisable mini-photo tiles but shift the art direction from pixel-art (Minecraft / Terraria / Stardew) to high-res photo pack (Conquest / Faithful). Project bible calls for pixel art, rejected. **Fix (shipped):** kept NEAREST; added a `_warn_if_not_pixel_art` heuristic to the builder that prints WARN for any source whose intra-cell variance suggests photo input; rewrote `tools/AI_TEXTURE_PROMPTS.md` to lead with pixel-art-specific generators (Retrodiffusion, PixelLab.ai, Aseprite) and demote NB / DALL-E to "fallback only." General rule for diffusion-AI asset pipelines: prefer tools whose output domain IS the pipeline's input format. Pixel-art textures from a general-purpose diffusion model is a forced fit; pixel-art textures from a pixel-art-specific diffusion model is its happy path. | #241
